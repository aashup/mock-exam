# Base URL for API calls. In dev, leave as /api so Vite's proxy forwards to
# the Laravel backend (http://localhost:8000) — see vite.config.ts.
# For a production build, set this to the real API origin, e.g.
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=/api
